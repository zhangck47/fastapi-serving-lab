# fastapi-serving-lab 面试技术 Q&A

这是一份随项目演进持续更新的面试题库。当前版本覆盖 Day 1～Day 10 已经实现、测试并能够从代码中证明的 Python 压测解析能力，以及 FastAPI 健康检查、Path 参数和 Query 参数接口。

> 面试表达原则：只把已经实现和测试通过的功能说成项目成果。当前完成了最小 FastAPI `/health` 和参数练习接口；聊天接口、AsyncIO、SSE 和 OpenAI 兼容网关仍在路线图中，完成对应阶段后再加入本题库。

## 一、项目概述与架构

### Q1：请用一分钟介绍当前项目。

**回答：**

`fastapi-serving-lab` 是我从 Python 基础逐步构建 OpenAI 兼容模型服务网关的连续工程项目。第一阶段完成了压测结果解析器：读取 JSON/JSONL、处理异常、使用数据类建模，并通过生成器惰性处理大文件。第二阶段已实现 FastAPI `GET /health`，以及同时接收 Path 和 Query 参数的练习路由；FastAPI 会根据类型标注完成转换和 422 校验。项目现有 32 个 pytest 测试，聊天接口、异步和流式能力仍会按路线图逐步实现。

### Q2：为什么先做压测解析器，而不是直接写 FastAPI？

**回答：**

模型服务网关最终仍依赖可靠的 Python 数据处理能力。如果直接写路由，我可能只会模仿框架代码，却无法解释文件、异常、类型、对象、生成器和测试。压测解析器与我的推理服务经历也相关：它使用 request ID、模型名、token、延迟和缓存状态等真实概念。这样能在一个连续项目里学习 Python，同时让这些能力以后自然进入 API 和网关内部。

### Q3：当前代码为什么拆成 `loaders`、`models`、`metrics` 和 `pipeline`？

**回答：**

- `loaders` 负责 I/O 和 JSON 解码。
- `models` 负责数据结构、字典转对象和对象汇总。
- `metrics` 保留早期字典版本的筛选和统计函数。
- `pipeline` 只负责编排已有能力。

这种拆分让每个模块只有一个主要变化原因。例如文件格式变化主要影响 loader，指标公式变化主要影响 model/metrics，而流程组合变化主要影响 pipeline。测试也能分别定位失败层级。

### Q4：为什么 `pipeline.py` 不直接重新实现文件读取和指标计算？

**回答：**

流水线的职责是组合，不是复制。重复实现会产生两套空行处理、异常信息和指标公式，长期容易不一致。当前流水线依次调用 `iter_benchmark_records()`、`benchmark_record_from_dict()` 和 `summarize_benchmark_record()`，每层仍可独立测试和复用。

### Q5：请描述一条记录的完整处理过程。

**回答：**

`iter_benchmark_records()` 在生成器被消费时打开 JSONL 文件，逐行读取并跳过空行，然后用 `json.loads()` 得到字典。`benchmark_record_from_dict()` 将字典转换成 `BenchmarkRecord`。`summarize_benchmark_record()` 计算总 token 数和每秒生成 token 数，得到 `BenchmarkSummary`。`iter_benchmark_summaries()` 使用 `yield` 将汇总对象逐条交给调用者，所以整个流程保持惰性。

### Q6：当前项目已经实现了哪些功能，哪些还没有？

**回答：**

已经实现 JSON/JSONL 文件解析、异常转换、坏行定位、数据类建模、指标汇总、生成器惰性读取、提前停止，以及 FastAPI `GET /health`、Path/Query 参数练习和对应 `TestClient` 测试。尚未实现聊天请求模型、`/v1/models`、`/v1/chat/completions`、异步后端调用、SSE、OpenAI schema、超时、取消、并发限制和限流等网关能力。它们在后续路线图中，当前不能算作已交付功能。

### Q7：为什么保留返回列表和返回生成器两种读取接口？

**回答：**

二者适合不同场景。列表接口使用简单，可以重复遍历和随机访问，适合小文件。生成器接口按需读取，额外内存基本不随记录数增长，并支持提前停止，适合大压测文件。保留两者也避免为了新能力破坏早期调用方。

## 二、Python 数据结构与控制流

### Q8：项目中为什么使用 `list` 保存记录？

**回答：**

列表保持文件顺序且允许追加，符合“一行一条记录”的顺序语义。早期 `load_benchmark_records()` 读取到一条就 `append()`，最终返回完整结果。缺点是内存随记录数增长，因此后来增加了生成器接口。

### Q9：为什么模型名去重使用 `set`，返回时又转换成 `tuple`？

**回答：**

`set` 适合保证元素唯一，但不能提供业务所需的稳定排序。因此先用集合去重，再用 `sorted()` 排序，最后转换成不可变的 `tuple`。稳定输出便于测试，也避免调用者无意修改结果。

### Q10：`dict` 与 `dataclass` 在项目中分别处于什么位置？

**回答：**

字典位于外部数据边界：JSON 解码后自然得到字典。进入业务逻辑后，字典被转换成 `BenchmarkRecord` 数据类。字典灵活但字段不明显，`dict[str, object]` 也让编辑器无法推断具体值类型；数据类集中声明字段和类型，属性访问更清晰，并自动提供比较和字符串表示。

### Q11：为什么必要字段用 `data["field"]`，可选字段用 `data.get()`？

**回答：**

必要字段缺失代表数据不完整，使用下标会立即抛出 `KeyError`，更早暴露问题。可选字段缺失是允许的，`get()` 可以返回 `None`。如果必要字段也使用 `get()`，坏数据可能带着 `None` 继续运行，在更远处才产生难以理解的错误。

### Q12：`for` 循环为什么能保持筛选结果的原始顺序？

**回答：**

`for` 按列表从前到后的顺序访问元素，满足条件时 `append()` 又按发现顺序加入新列表，因此保留下来的记录之间仍维持原始相对顺序。

### Q13：`continue` 在 JSONL 读取里起什么作用？

**回答：**

当前行经过 `strip()` 后为空时，`continue` 立即结束这一轮，不执行 `json.loads()`，直接进入下一行。这样空行不会被当成损坏 JSON，同时 `enumerate` 生成的真实文件行号仍然保留。

## 三、函数、类型标注和数据类

### Q14：类型标注有什么价值？

**回答：**

类型标注记录接口约定，让读代码的人和编辑器知道函数需要什么、返回什么，也为静态检查提供基础。例如 `Iterator[BenchmarkSummary]` 明确表示结果按需逐条产生汇总对象。它提升可读性和重构安全性，但不是运行时验证。

### Q15：Python 类型标注会自动阻止错误类型吗？

**回答：**

默认不会。即使字段标为 `int`，普通 Python 运行时仍可能收到字符串。当前阶段使用类型标注表达意图，并通过测试保护已知输入；后续 FastAPI 阶段会使用 Pydantic 对外部请求做运行时解析和校验。

### Q16：为什么使用 `@dataclass`？

**回答：**

记录和汇总主要用于保存数据，`dataclass` 能根据字段声明自动生成 `__init__`、`__eq__` 和便于调试的 `__repr__`，减少样板代码。测试可以直接比较两个数据类对象。当前没有使用复杂继承或自定义魔法方法，保持结构简单。

### Q17：`BenchmarkRecord` 和 `BenchmarkSummary` 为什么分开？

**回答：**

原始记录保存输入事实，例如 prompt token、completion token 和 latency；汇总保存派生结果，例如 total token 和 tokens per second。分开后不会把输入字段和计算字段混在同一个可变对象里，也使计算函数的输入输出更明确。

### Q18：`cache_hit: bool | None` 的三个值分别是什么意思？

**回答：**

`True` 表示明确命中缓存，`False` 表示明确未命中，`None` 表示没有提供信息。`None` 不能等同于 `False`，否则“未知”会被统计成“未命中”，使缓存指标失真。

### Q19：为什么有默认值的字段必须放在无默认值字段之后？

**回答：**

数据类生成的初始化函数遵循普通函数参数规则：必填参数不能出现在带默认值参数之后。否则 Python 无法形成合法的函数签名，会报 `non-default argument follows default argument`。

### Q20：当前数据类有什么局限？

**回答：**

它不自动验证运行时类型、数值范围和跨字段约束。例如 `latency_ms=0` 会在吞吐量计算时除零，负 token 也不会被自动拒绝。后续可以增加显式校验或用 Pydantic 建立外部数据边界，但阶段 1 没有过度设计。

## 四、模块、包和环境

### Q21：模块和包分别是什么？

**回答：**

一个 `.py` 文件通常是模块，例如 `loaders.py`。包含多个模块并具有包结构的目录是包，例如 `serving_lab/`。包帮助组织命名空间并提供相对导入。

### Q22：`__init__.py` 在当前项目中有什么作用？

**回答：**

它把常用类和函数重新导出到 `serving_lab` 顶层，让调用者依赖稳定入口，而不必知道内部文件位置。内部模块未来重构时，只要顶层接口保持不变，调用方就不需要修改。

### Q23：`__all__` 会自动导入名称吗？

**回答：**

不会。名称必须先真实导入或定义，`__all__` 只是公开接口声明，主要影响 `from package import *`，也向读代码的人和文档工具表达包希望公开哪些名称。它不是安全控制。

### Q24：相对导入中的 `.` 表示什么？

**回答：**

表示当前包。例如 `from .models import BenchmarkRecord` 会从当前 `serving_lab` 包的 `models` 模块导入，而不是寻找一个顶级 `models` 模块。

### Q25：为什么使用虚拟环境？

**回答：**

虚拟环境隔离项目的 Python 解释器和第三方依赖，避免其他项目安装不同 pytest 或 FastAPI 版本造成冲突。它不会改变源代码，只影响运行时使用哪个解释器和依赖目录。

### Q26：为什么采用 `src/` 布局？

**回答：**

`src` 布局将可导入包与仓库根目录分开，减少测试因为当前工作目录而意外导入未安装代码的风险，也更接近真实打包后的导入方式。`pyproject.toml` 配置 setuptools 从 `src` 发现包。

## 五、文件、JSON 和异常

### Q27：JSON 和 JSONL 有什么区别？

**回答：**

JSON 文件整体是一个合法 JSON 值；JSONL 的每个非空行都是一个独立 JSON 值。JSON 适合整体配置或一个结构，JSONL 适合日志、压测记录和逐行处理，单条损坏也能报告具体行号。

### Q28：为什么明确指定 UTF-8？

**回答：**

Windows 默认编码可能因系统区域设置不同而变化。明确指定 UTF-8 可以让相同文件在不同环境得到一致结果，避免中文或特殊字符解码失败。

### Q29：为什么使用 `pathlib.Path`？

**回答：**

`Path` 让路径成为对象，提供 `read_text()`、`open()` 和 `/` 路径组合等接口，比手工拼字符串更清晰，也能跨平台表达路径操作。

### Q30：`json.load()` 与 `json.loads()` 有什么区别？

**回答：**

`json.load()` 从文件对象读取 JSON，`json.loads()` 从字符串或字节解码 JSON。当前项目先读取文本或逐行得到字符串，再调用 `json.loads()`，这样能控制文件生命周期并保留 JSONL 行号。

### Q31：文件存在是否代表内容一定是合法 JSON？

**回答：**

不代表。文件存在只说明路径和读取阶段成功；内容可能为空、缺引号、括号不匹配或有多余逗号。因此文件读取错误和 JSON 解码错误需要分别处理。

### Q32：为什么使用 `with` 打开文件？

**回答：**

文件对象是上下文管理器。离开 `with` 时，无论正常完成还是发生异常，退出逻辑都会关闭文件。生成器被显式 `close()` 时也会从暂停点退出并触发清理。

### Q33：为什么使用 `raise ... from exc`？

**回答：**

它建立新业务异常与原始底层异常之间的因果链。调用者看到的是包含路径和行号的清晰错误，调试时仍能追踪到底层 `JSONDecodeError` 或 `FileNotFoundError`。

### Q34：为什么坏行行号要用 `enumerate(file, start=1)`？

**回答：**

它对文件中的每一行计数，包括之后跳过的空行，并且从 1 开始，与编辑器行号一致。如果只在成功解码后增加计数，空行会被漏掉，报告的就不是实际文件位置。

### Q35：为什么把 `JSONDecodeError` 转换成 `ValueError`？

**回答：**

loader 对外表达的是“压测文件的某行内容不合法”，这是调用者关心的业务语义。底层异常仍通过异常链保留。这样上层不必依赖 JSON 库的具体异常细节。

### Q36：为什么不使用一个很大的 `except Exception`？

**回答：**

捕获所有异常会把编程错误也包装成文件错误，降低可诊断性。项目只捕获能够正确解释的具体异常，其他意外错误继续暴露，便于尽早修复。

## 六、迭代器、生成器和资源

### Q37：调用生成器函数时为什么没有立即读取文件？

**回答：**

函数体中包含 `yield` 时，调用函数只创建生成器对象，不执行函数体。第一次 `next()` 才从开头运行，打开文件并处理到第一个 `yield`。

### Q38：`yield` 和 `return` 有什么区别？

**回答：**

`return` 返回结果并结束函数；`yield` 返回一个值后暂停，保存局部变量和执行位置，下一次 `next()` 可以继续。生成器最终自然结束时会触发 `StopIteration`。

### Q39：生成器为什么节省内存？

**回答：**

它只保留当前执行状态和当前记录，不创建包含全部记录的列表。对于 N 条记录，列表额外内存通常随 N 增长，而逐条生成的额外数据内存近似常量。文件内容本身仍在磁盘上。

### Q40：`list(generator)` 会发生什么？

**回答：**

它会不断调用 `next()` 直到生成器结束，把所有结果收集成列表。这样使用方便，但会完整读取文件并占用列表内存，失去提前停止优势。

### Q41：生成器为什么能在后续坏行之前提前停止？

**回答：**

每次 `next()` 只执行到下一个 `yield`。得到第一条记录后，代码暂停在第一条之后，还没有读取第二行。因此第二行损坏只有在再次消费生成器时才会出现。

### Q42：生成器提前停止时为什么要考虑 `close()`？

**回答：**

生成器暂停在 `with` 内时仍持有打开的文件。如果调用者保留生成器但不再消费，文件可能继续打开。调用 `close()` 会向生成器发出关闭信号，使其退出 `with` 并立即释放文件资源。

### Q43：生成器可以重复遍历吗？

**回答：**

不能。生成器是一次性迭代器，消费后不会自动回到开头。需要重新遍历时应重新调用生成器函数创建新对象。列表则可以重复遍历。

### Q44：当前端到端流水线为什么仍然是惰性的？

**回答：**

外层 `iter_benchmark_summaries()` 本身使用 `yield`，并逐条消费内层 `iter_benchmark_records()`。它没有调用 `list()`，也没有预先收集所有对象，因此每次请求一个 summary 才向下游请求一条原始记录。

## 七、指标与业务语义

### Q45：当前计算了哪些指标？

**回答：**

`total_tokens = prompt_tokens + completion_tokens`，`tokens_per_second = completion_tokens / (latency_ms / 1000)`。第二个指标表示当前简化场景下的生成吞吐量，输入延迟单位是毫秒，所以先转换成秒。

### Q46：为什么 tokens per second 不使用 prompt token？

**回答：**

当前指标定义关注输出生成速率，所以分子使用 completion token。prompt token 属于输入处理，不应混入输出生成速率。更严格的推理性能分析会分别关注 TTFT、TPOT、prefill 和 decode，但这些尚未在当前代码实现。

### Q47：prompt token 为零会导致除零吗？

**回答：**

不会。分母是延迟秒数，不是 prompt token。Day 8 的错误就是错误地把零 prompt 与无限吞吐联系起来。只有 `latency_ms=0` 才会导致除零。

### Q48：如果 `latency_ms=0`，当前会怎样？如何改进？

**回答：**

当前会抛出 `ZeroDivisionError`。改进时应在数据边界明确约束延迟必须大于零，并返回清晰的校验错误。后续 Pydantic 模型可以承担外部请求/数据校验；也可以在转换或计算函数中显式检查。

### Q49：浮点数测试是否总能直接使用 `==`？

**回答：**

不总是。当前 12.5 和 20.0 等值可以被二进制浮点准确表示，所以直接比较稳定。遇到 1/3 等无法准确表示的结果时，应使用 `pytest.approx()` 或明确容差。

### Q50：TTFT、TPOT 和当前 tokens per second 有什么区别？

**回答：**

TTFT 是从请求开始到首个 token 的时间；TPOT 通常描述首 token 之后每个输出 token 的平均生成时间；当前 tokens per second 是用总 completion token 除以给定总延迟的简化吞吐指标。当前代码没有单独采集首 token 时间和逐 token 时间，不能把它宣称为 TTFT 或 TPOT 实现。

## 八、pytest 与质量保障

### Q51：为什么从第一天就写 pytest？

**回答：**

测试让每个知识点都有可执行结果，并在拆模块、改数据模型和增加生成器时保护旧行为。否则代码“看起来能运行”，但重构时很难确认是否破坏前几天功能。

### Q52：参数化测试解决了什么问题？

**回答：**

它把共同的准备、执行和断言逻辑写一次，通过参数列表覆盖不同输入。新增边界场景只添加数据，不复制函数。每组参数仍是独立用例，失败报告会指出具体参数组。

### Q53：正常、边界和异常测试有什么区别？

**回答：**

- 正常测试验证典型业务路径，例如普通 token 记录得到正确汇总。
- 边界测试验证合法极端输入，例如 completion 为零。
- 异常测试验证错误输入的失败方式，例如损坏 JSONL 报告准确行号。

三者分别防止主逻辑错误、边界假设错误和错误处理回归。

### Q54：`tmp_path` 有什么价值？

**回答：**

它为每个测试提供隔离的临时目录，测试可以真实写入 JSON/JSONL 文件而不污染仓库，也避免不同测试使用同一文件互相影响。

### Q55：项目曾经遇到的 Windows pytest 权限问题是什么？

**回答：**

Codex 测试进程和用户终端使用不同真实 Windows 登录身份，但环境变量都可能显示为同一个用户名。pytest 临时目录名称相同，实际 ACL 所有者却不同，另一身份无法访问或清理，因此出现 WinError 5。

### Q56：这个权限问题如何从根因解决？

**回答：**

`tests/conftest.py` 使用 `os.getlogin()` 获取真实登录身份，并为每个身份设置不同的项目内 basetemp，例如 `.pytest-tmp-zck` 和 `.pytest-tmp-CodexSandboxOffline`。`.gitignore` 忽略这些目录。重点是隔离 ACL 所有权，而不是反复删除同一个受限目录。

### Q57：为什么错误发生在 `tmp_path` 阶段时，不应该先怀疑业务代码？

**回答：**

测试堆栈显示失败发生在 fixture 准备阶段，业务函数还没有被调用。调试应先定位堆栈中最早属于自己代码的位置；如果完全没有进入业务函数，就应检查测试环境和 fixture。

### Q58：当前有哪些回归保护？

**回答：**

32 个测试覆盖字典统计、JSON/JSONL 读取与异常、数据类转换、生成器惰性行为、流水线和参数化指标，以及 `/health` 和参数练习路由。接口测试验证状态码、JSON、HTTP 方法、Path 参数、Query 默认值和错误类型的 422。每次提交前运行完整套件，而不是只运行当天测试。

### Q59：当前测试还缺什么？

**回答：**

阶段 1 尚未覆盖错误字段类型、缺少必要字段、负 token、零或负延迟、超大文件的性能测量，也没有静态类型检查和覆盖率门槛。后续应根据风险逐步增加，而不是第一版一次加入所有工具。

## 九、排错与设计取舍

### Q60：Day 7 的 closed file 错误是怎样产生的？

**回答：**

最初把三个 TODO 当成三段并列实现：第一段已在 `with` 中完整循环并产出记录，离开 `with` 后第二段又尝试遍历同一个 `file`。此时文件已经关闭，完整消费生成器时出现 `ValueError: I/O operation on closed file`。修复方式是保留一套正确嵌套流程。

### Q61：为什么 Day 7 的提前停止测试当时可能通过，但完整列表测试失败？

**回答：**

提前停止测试只调用一次 `next()`，在第一个 `yield` 后就关闭生成器，没有运行到后面的重复循环。`list()` 会继续消费直到结束，因此进入了离开 `with` 后的错误代码。这说明测试需要同时覆盖提前停止和完整消费。

### Q62：Day 8 为什么出现 `10.0 != inf`？

**回答：**

测试把 prompt token 为零误认为吞吐量分母为零。但公式的分母是延迟秒数。输入为 5 个 completion token 和 500ms 时，结果是 `5 / 0.5 = 10.0`。最终按题目改为 250ms，对应 20.0。

### Q63：代码能运行但风格不好时，为什么仍要修改？

**回答：**

例如类型标注缺少空格不会影响运行，但会降低可读性和一致性。工程质量不仅是通过测试，还包括让下一位维护者容易阅读、审查和修改。风格修改不应掩盖逻辑变化，因此最好保持小而明确。

### Q64：为什么没有在第一阶段加入复杂抽象或设计模式？

**回答：**

当前规模用函数、小模块和数据类已经能清晰表达职责。提前引入抽象基类、插件系统或复杂工厂会增加理解和测试成本，却没有真实变化需求。项目遵循按需求演进、避免为了展示技术而过度设计。

## 十、Git 与交付流程

### Q65：每天为什么使用功能分支和 PR？

**回答：**

功能分支隔离当天未完成代码，`main` 始终保持已通过测试的状态。PR 提供变更范围、原因、验证结果和可审查记录。合并后删除短期分支，避免分支列表长期堆积。

### Q66：为什么采用 fast-forward 合并？

**回答：**

每天的分支通常只比最新 `main` 多一个线性提交。确认远端 `main` 没有新变化后，fast-forward 让 `main` 直接移动到该提交，不制造额外 merge commit，历史简洁。如果不能 fast-forward，则说明基线发生变化，应先停止并检查冲突。

### Q67：提交前做哪些检查？

**回答：**

检查工作区状态和完整 diff，确认没有无关文件；运行完整 pytest；运行 `git diff --check`；只用明确路径暂存；检查暂存统计后再提交。推送后验证 PR 的 base、head 和提交 SHA，合并后再次测试并确认 `main` 与远端同步。

### Q68：为什么不直接在 `main` 上每天开发？

**回答：**

学习中的 TODO 和预期失败测试会让 `main` 长时间处于红色状态，也难以区分已完成与未完成内容。短期分支使实验可隔离，并提供清晰的通关边界。

## 十一、面试追问与改进方向

### Q69：如果文件有一千万条记录，当前哪个接口更合适？

**回答：**

使用 `iter_benchmark_records()` 或 `iter_benchmark_summaries()`。它们逐条处理，额外数据内存主要与当前记录大小相关。不能在调用处立即套 `list()`，否则仍会把全部结果放入内存。

### Q70：如果只需要前 100 条成功记录，如何利用当前设计？

**回答：**

逐条消费生成器，记录成功条数，到 100 条后停止，并关闭生成器。这样后续文件内容不会被读取。当前项目还没有实现专门的 `islice` 或过滤生成器，可以在真实需求出现时增加一个小的组合层。

### Q71：如果 JSON 语法正确但字段缺失，会发生什么？

**回答：**

`json.loads()` 会成功；转换数据类时访问必要字段会抛出 `KeyError`。当前还没有把它转换成统一的输入校验错误。后续应在外部数据边界加入明确 schema 校验，并为缺失字段编写测试。

### Q72：如果字段类型错误，例如 token 是字符串，会发生什么？

**回答：**

普通数据类不会自动阻止。错误可能在加法或除法时出现，错误位置不够靠近输入边界。这是后续引入 Pydantic 的理由之一：在数据进入业务逻辑前完成类型解析和校验。

### Q73：如何让当前项目支持 CSV？

**回答：**

新增一个只负责 CSV 解码的 loader，让它产出与现有转换边界兼容的字典，然后复用 `benchmark_record_from_dict()` 和汇总流水线。不应把 CSV 分支塞进指标函数。

### Q74：如何增加新的指标而不破坏现有代码？

**回答：**

先明确新指标需要哪些原始字段，再扩展数据模型和汇总函数，保持 loader 只负责解析。为正常、边界和异常输入增加测试。如果要保持对旧调用方兼容，应谨慎处理新必填字段和数据类构造参数。

### Q75：FastAPI 层今后如何复用当前解析代码？

**回答：**

当前 `/health` 只返回服务状态，不需要解析数据。后续路由应作为输入输出边界，调用服务或 pipeline 层，而不是把解析和指标逻辑复制到路由中。现有数据类可以继续承担内部数据表达，之后 Pydantic 模型负责 HTTP 请求/响应校验，两者职责明确分开。

### Q76：当前项目最值得写进简历的阶段 1 表述是什么？

**回答：**

可以写：构建可测试的 Python 压测结果解析流水线，支持 JSON/JSONL、准确坏行定位、数据类建模和生成器惰性处理；使用 pytest 覆盖正常、边界、异常与参数化场景，并增加 FastAPI 健康检查入口和 HTTP 级测试。不要因为完成一个 `/health` 路由，就声称已经实现完整模型网关或异步流式服务。

## 十二、Day 9：HTTP、FastAPI 与接口测试

### Q77：HTTP 请求和响应分别是什么？

**回答：**

请求是客户端发送给服务端的消息，表达希望访问什么以及执行什么操作；响应是服务端处理后返回客户端的消息。今天的请求包含 GET 方法和 `/health` 路径，响应包含状态码、Header 和 JSON 响应体。请求方向是客户端到服务端，响应方向相反。

### Q78：为什么健康检查使用 GET？

**回答：**

健康检查只读取服务当前状态，不创建或修改资源，因此符合 GET 的查询语义。调用方、测试和监控系统看到 `GET /health` 就能理解它是一次状态读取。

### Q79：FastAPI 中的路由是什么？

**回答：**

路由是一条“HTTP 方法 + URL 路径 → Python 处理函数”的映射。`@app.get("/health")` 表示 GET 方法访问 `/health` 时调用紧随其后的 `health()`。方法和路径必须同时匹配。

### Q80：`FastAPI` 与 `FastAPI()` 有什么区别？

**回答：**

`FastAPI` 是框架提供的类，`FastAPI()` 是调用这个类创建应用对象。路由通过应用对象的 `.get()` 等方法注册，`TestClient` 也需要接收这个应用对象，而不是类本身。

### Q81：为什么函数返回 Python 字典，客户端却收到 JSON？

**回答：**

处理函数返回 `{"status": "ok"}` 后，FastAPI 会把可 JSON 化的 Python 数据序列化为 JSON 响应，并设置相应内容类型。测试中的 `response.json()` 又把响应 JSON 解码成 Python 对象，便于断言。

### Q82：200、404 和 405 分别表示什么？

**回答：**

- 200 表示请求成功处理。
- 404 表示没有路由匹配目标路径。
- 405 表示路径对应的资源存在，但当前 HTTP 方法不被允许。

在本项目中，`GET /health` 返回 200；`POST /health` 因只注册了 GET 而返回 405。

### Q83：为什么用 `TestClient`，而不是只直接调用 `health()`？

**回答：**

直接调用只能证明函数返回了一个字典。`TestClient` 会经过 FastAPI 的路由匹配和响应转换，因此还能验证路径是否注册、GET 是否允许、HTTP 状态码和最终 JSON 是否正确。它不需要启动真实端口，适合快速接口测试。

### Q84：为什么 Day 9 的测试函数和路由函数都使用普通 `def`？

**回答：**

Day 9 只学习同步的最小接口，不提前引入异步。FastAPI 支持普通 `def` 路由，`TestClient` 也能在普通 pytest 函数中同步调用。`async def`、`await` 和异步测试会在 AsyncIO 阶段系统学习。

### Q85：为什么接口测试要分别断言状态码和响应体？

**回答：**

状态码验证 HTTP 层的成功或失败语义，响应体验证业务数据。只检查 200 可能放过错误内容，只检查 JSON 也可能忽略错误状态，因此两者需要分别验证。

### Q86：完成 `/health` 是否代表 FastAPI 网关已经完成？

**回答：**

不代表。它只证明项目已经建立最小 FastAPI 应用、GET 路由和接口测试入口。请求体、Pydantic 校验、模型列表、聊天接口、统一异常、异步后端、SSE、超时与并发限制仍未实现。

## 十三、Day 10：URL、Path 参数与 Query 参数

### Q87：URL 中路径和查询字符串如何区分？

**回答：**

路径位于主机和端口之后、问号之前，例如 `/debug/models/mock-llm`。查询字符串位于 `?` 之后，例如 `limit=3`。路径通常定位资源或功能，Query 通常表达过滤、分页或其他查询选项。

### Q88：什么是 Path 参数？

**回答：**

Path 参数是路径中会变化的片段。FastAPI 使用花括号声明，例如 `/debug/models/{model_name}`。请求 `/debug/models/mock-llm` 时，动态值 `mock-llm` 会传给同名函数参数 `model_name`。

### Q89：为什么路由中的 `{model_name}` 必须与函数参数同名？

**回答：**

FastAPI 根据名称把路由捕获的值注入处理函数。名称一致时，框架知道动态路径值应传给哪个参数；名称不一致会导致参数无法按声明正确绑定。

### Q90：FastAPI 如何区分 Path 参数和 Query 参数？

**回答：**

出现在路由路径模板中的函数参数是 Path 参数；没有出现在路径模板中的普通参数会被解释为 Query 参数。在 `inspect_model_parameters(model_name: str, limit: int = 1)` 中，`model_name` 出现在 `{model_name}`，而 `limit` 没有，所以两者来源不同。

### Q91：`limit: int = 1` 同时表达了什么？

**回答：**

`int` 告诉 FastAPI 把 URL 中的文本转换并校验为整数；`= 1` 表示客户端省略 `limit` 时使用默认整数 1。因此它是一个可省略、但一旦提供就必须能转换成整数的 Query 参数。

### Q92：为什么 URL 中的 `limit=3` 进入函数后是整数？

**回答：**

URL 参数最初是文本。FastAPI 读取函数的 `int` 类型标注，在调用函数前把文本 `"3"` 转成整数 `3`。这个转换由框架完成，处理函数不需要手工调用 `int()`。

### Q93：为什么 `limit=many` 返回 422？

**回答：**

路由和参数位置都能识别，但文本 `many` 无法满足 `int` 类型声明。FastAPI 在处理函数运行前终止请求并返回 422 校验错误。它与 404 不同：404 是路径没有匹配，422 是路径已匹配但输入内容不合法。

### Q94：为什么不应该在处理函数内手工解析 `int(limit)`？

**回答：**

把类型写进函数签名后，FastAPI 能统一完成转换、错误响应和自动文档。如果每个路由都手工解析，会重复 `try/except`，错误结构也容易不一致，并且函数内部收到的类型不再明确。

### Q95：Day 10 的字符串错误是怎样产生的？

**回答：**

FastAPI 已把 Query 文本转换为整数，但最初返回时又写了 `str(limit)`，把整数重新变成字符串。因此 JSON 中出现 `"limit": "3"`，而测试期望数字 `3`。修复时直接返回 `limit`，并让返回类型标注允许字符串和整数混合值。

### Q96：为什么同时测试显式值、默认值和错误类型？

**回答：**

显式值证明 Path 与 Query 能正确提取和转换；默认值证明省略可选 Query 时行为稳定；错误类型证明无效输入会在业务函数之前被统一拒绝。三者分别覆盖正常、边界和异常路径。

## 十四、推荐的现场讲解顺序

面试官要求打开代码时，可以按以下顺序讲：

1. 从 `app.py` 展示 `/health` 和参数练习路由。
2. 用 `tests/test_health.py` 和 `tests/test_parameters.py` 说明状态码、方法、Path、Query 和 422。
3. 从 `pipeline.py` 展示内部数据流。
4. 进入 `loaders.py` 解释 JSONL、异常和惰性读取。
5. 进入 `models.py` 解释数据类、可选值和指标。
6. 进入流水线与生成器测试展示参数化、提前停止和惰性异常。
7. 最后主动说明当前边界与下一阶段计划。

这样先讲“系统如何工作”，再讲局部实现和测试，比从第一行代码开始逐句解释更像工程面试表达。
