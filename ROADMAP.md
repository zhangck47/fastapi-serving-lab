# fastapi-serving-lab 学习路线

## 总体节奏

核心路线共 36 天，每天 60～90 分钟。默认时间分配：10～15 分钟理解必要知识，30～45 分钟独立编码，15～20 分钟运行测试和排错，5～10 分钟复盘。当天代码、测试和复习问题未通过时，不进入下一天。

每一天最多聚焦 2～3 组新概念。测试从第 1 天开始，始终与功能一起演进。

## 当前进度

- Day 1：已通过。
- Day 2：代码审查与自动化测试已通过（2026-08-11，`5 passed`），复习问题待回答。
- Day 3：尚未开始。

## 阶段 1：Python 核心基础（第 1～8 天）

| 天数 | 新概念（最多 3 组） | 项目增量 | 测试与通关点 |
| --- | --- | --- | --- |
| Day 1 | 基本类型与变量；`list`/`dict`；函数与类型标注 | 汇总一条已经脱敏、已经解码的压测记录 | 给定记录能得到正确的总 token 数和吞吐量 |
| Day 2 | 条件判断；`for` 循环；`tuple`/`set` | 筛选多条成功记录并汇总模型名 | 覆盖成功、失败和重复模型 |
| Day 3 | 异常；文件路径；JSON 解码 | 从一个 JSON 文件读取压测结果 | 覆盖文件不存在和 JSON 格式错误 |
| Day 4 | `with` 上下文管理器；JSONL；逐行处理 | 读取多行压测结果并定位坏行 | 覆盖空文件、合法行和损坏行 |
| Day 5 | 模块；包；虚拟环境 | 将读取、统计和命令入口拆成小模块 | 导入路径清晰，模块职责单一 |
| Day 6 | 类与对象；`dataclass`；可选值 | 用数据类表达压测记录和汇总结果 | 覆盖对象创建和字段转换 |
| Day 7 | 迭代器；生成器；惰性处理 | 逐条产出大文件记录，避免一次加载全部数据 | 验证生成顺序和提前停止 |
| Day 8 | pytest 断言；参数化；阶段复盘 | 完成压测结果解析器小版本 | 正常、边界、异常测试全部通过，并能口述代码职责 |

阶段通关：能独立处理 JSON/JSONL 和文件；能拆分模块、写类型标注、处理异常并编写 pytest 测试。

## 阶段 2：HTTP、FastAPI 和 pytest（第 9～15 天）

| 天数 | 新概念（最多 3 组） | 项目增量 | 测试与通关点 |
| --- | --- | --- | --- |
| Day 9 | HTTP 请求/响应；GET；FastAPI 路由 | 第一个 `/health` 接口 | 从第一个接口开始使用 `TestClient` |
| Day 10 | URL；Path 参数；Query 参数 | 增加用于观察参数来源的练习路由 | 覆盖合法参数与自动校验失败 |
| Day 11 | POST；Body；Pydantic 模型 | 建立最小聊天请求模型 | 覆盖请求体成功与字段缺失 |
| Day 12 | Header；状态码；响应模型 | 让响应结构和状态码明确可验证 | 测试 Header、200 和 4xx |
| Day 13 | `/v1/models`；依赖注入 `Depends`；Swagger | 返回 Mock 模型列表并提取共享依赖 | 测试模型列表和依赖行为 |
| Day 14 | 自定义异常；异常处理器；统一错误 JSON | 建立统一错误响应，不暴露内部异常 | 覆盖已知错误和参数校验错误 |
| Day 15 | 路由组织；测试夹具；阶段复盘 | 完成非流式 `/v1/chat/completions` 骨架 | 每个接口有测试，能解释 GET/POST 和常用状态码 |

阶段通关：能独立实现 GET/POST，定义请求与响应模型，解释状态码并测试接口。

## 阶段 3：AsyncIO 异步编程（第 16～22 天）

| 天数 | 新概念（最多 3 组） | 项目增量 | 测试与通关点 |
| --- | --- | --- | --- |
| Day 16 | 同步/异步；I/O 密集；协程 | 将 Mock 等待过程改成 `async def` | 能画出调用方遇到 `await` 后的执行过程 |
| Day 17 | `Task`；事件循环；`gather` | 并发调用多个 Mock 后端 | 用耗时和结果证明并发发生 |
| Day 18 | `httpx.AsyncClient`；异步 HTTP；客户端生命周期 | 用异步客户端访问 Mock 后端 | 覆盖多个后端成功返回 |
| Day 19 | timeout；超时异常；清理 | 为后端请求设置明确超时 | 测试成功与超时两条路径 |
| Day 20 | cancellation；`CancelledError`；传播 | 支持主动取消仍在等待的请求 | 测试取消能到达后端任务 |
| Day 21 | `Semaphore`；排队；最大并发 | 限制同时运行的 Mock 推理数 | 测试峰值并发不超过限制 |
| Day 22 | 异步上下文管理器；资源释放；阶段复盘 | 整理异步客户端和并发控制生命周期 | 成功、超时、取消测试全通过，能解释异步适用场景 |

阶段通关：能解释 `async`/`await`，实现超时、取消和最大并发限制，并说明异步为何适合模型服务 I/O。

## 阶段 4：SSE 流式响应（第 23～27 天）

| 天数 | 新概念（最多 3 组） | 项目增量 | 测试与通关点 |
| --- | --- | --- | --- |
| Day 23 | 迭代生成；Generator；Async Generator | Mock 后端逐块生成 token 文本 | 测试块顺序和结束条件 |
| Day 24 | `StreamingResponse`；普通/流式响应；分块读取 | 增加最小流式路由 | 测试客户端能收到多个块 |
| Day 25 | SSE 事件；`data: ...\n\n`；结束标记 | 输出合法 SSE 数据帧和 `[DONE]` | 自动解析并验证每个事件 |
| Day 26 | 客户端断开；取消生成；`finally` 清理 | 断开后停止 Mock 生成 | 测试生成任务被停止且资源释放 |
| Day 27 | 流式异常；响应已开始；背压基础 | 明确流开始前后两类错误策略 | 流式测试全通过，能解释背压和错误边界 |

阶段通关：能解释流式与非流式差异，独立实现 Async Generator，并正确处理客户端断开。

## 阶段 5：OpenAI 兼容模型服务网关（第 28～33 天）

| 天数 | 新概念（最多 3 组） | 项目增量 | 测试与通关点 |
| --- | --- | --- | --- |
| Day 28 | OpenAI 风格 schema；角色消息；兼容边界 | 固化请求、非流式响应和模型列表模型 | 对关键字段做兼容性测试 |
| Day 29 | 后端接口；适配器；Mock 实现 | 路由不再直接依赖 Mock 细节 | 用替身后端测试适配器边界 |
| Day 30 | 请求生命周期；非流式聚合；usage | 完成 `stream=false` | 验证内容、finish reason 和 usage |
| Day 31 | 流式 chunk schema；增量 delta；结束事件 | 完成 `stream=true` | 验证 chunk 顺序和 `[DONE]` |
| Day 32 | request ID；结构化日志；上下文传递 | 每次请求可被日志关联 | 测试响应头、响应体和日志使用同一 ID |
| Day 33 | 网关策略组合；超时/取消；最大并发 | 将已有稳定性能力接入统一链路 | 并发、超时、取消的接口级测试通过 |

阶段通关：网关具备 `/health`、`/v1/models`、`/v1/chat/completions`，支持流式与非流式、Mock 后端、适配器、request ID、日志、超时、取消、并发限制和统一异常。

## 阶段 6：稳定性、测试和项目交付（第 34～36 天）

| 天数 | 新概念（最多 3 组） | 项目增量 | 测试与通关点 |
| --- | --- | --- | --- |
| Day 34 | 测试分层；fixture；故障注入基础 | 整理单元测试与接口测试，模拟后端故障 | 核心成功和失败分支均有自动化测试 |
| Day 35 | 配置边界；启动/关闭；可复现运行 | 清理配置和应用生命周期 | 新环境按文档可启动且测试通过 |
| Day 36 | 请求全链路复盘；项目表达；面试追问 | 完善 README、架构说明和简历要点 | 能演示项目并解释超时、排队、取消、并发限制 |

最终通关：项目可运行、测试可通过、README 可复现；能解释一次请求的完整生命周期，并能接受围绕设计取舍的追问。

## 通关后的可选增量

限流、重试、熔断、Prometheus 指标、Docker、系统化压测和更完整的故障注入属于第二轮迭代。它们不会提前塞入第一版，也不会在同一天同时引入。
